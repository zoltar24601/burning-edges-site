# Burning Edges — Master Project Log

**Purpose:** Single source of truth for all Burning Edges projects. Written so a fresh chat, Claude Code, or future-me can pick up cold without re-deriving decisions from chat history. Update as work happens; the repo copy is canonical.

**Site:** burningedgesdfs.com (Netlify static, drag-drop deploy) · **Repo:** `zoltar24601/burning-edges-site` · **Backend:** Supabase · **Stack:** vanilla HTML/CSS/JS · **Owner:** John

**Last updated:** 2026-09-02

---

## How to use this doc

- **Two-track workflow:** *This chat interface* handles pricing runs and data judgment (parsing sales, dedup, valuation calls). *Claude Code* (running locally against the repo) handles backend/plumbing (engine commits, Supabase wire-up, deploys, optimizer work).
- **Canonical rule:** The **repo** is the source of truth for code (engine, pages, functions) AND for this doc. When a pricing decision is made in chat, it lands here. When Claude Code changes the backend, that lands here too. One writer at a time to avoid drift.
- **The failure mode this doc prevents:** decisions and logic living only in chat history, so a too-full-chat handoff loses the *why*. Every non-obvious call goes in the Decision Log below with a date.

---

## Design system (shared across projects)

- Dark pine-green palette: `#061F18` / `#0A2B21`, cream `#EDE7D5`, accent greens/ambers/reds
- Golf-specific: fairway-neon `#B8F04A`, amber `#F2C14E`, red `#E5604C`
- Barlow Condensed Bold headers
- Twitter-ready graphic output sizing (golf: 1600×1000px)
- **GOTCHA:** Keep function/text files ASCII-only — em-dashes corrupt GitHub web-editor pastes. Function files need explicit `.js` extensions.

---

# PROJECT 1 — PBC (Panini Blockchain) Analytics — 2026 Prizm World Cup Soccer

The flagship. Pricing engine + pack EV + Value My Card + whale tracker for 2026 Prizm World Cup Soccer NFTs.

## Current state (as of last pricing run, 2026-07-31)
- **Pack counts:** 7,217 base / 2,141 FOTL remaining (typed in this run; ~40% of base / ~47% of FOTL packs opened since 07-09)
- **Book value:** base $88.96 → predicted ~$222 · FOTL $139.43 → predicted ~$349
- **Methodology (changed this run):** CLEAN canonical `pbc_engine.py` output — persistent overrides applied (Messi Gold /10 $50k, Mbappe Black/Nebula /1 $60k), but **NO stale offers file and NO +10% Messi/Mbappe/Yamal bump** (both were wrapper-only, not in the engine). The prior 07-09 live number ($127 → $318) used with-offers + the bump; John chose the clean version on 2026-07-31 (offers file 5 weeks stale; "arguably more honest"). So part of the $127→$89 drop is packs sold, part is this methodology change.
- **Mint:** base $25 / FOTL $150 · **Insert odds:** 35% · **Predicted multiplier:** 2.5x
- **Sales DB:** 14,848 unique sales (`burning_edges_sales_db.json`), unchanged this run (no new sales files sent — only remaining counts updated from the report)
- **Current report:** Panini Collection Report `__15_`, **White Sparkle rows excluded** (separate pack product — do NOT include in the calc). Typed-in pack counts authoritative over report-implied.
- **Published as:** `pbc_snapshots` id=11 (`published=true`; id=10 retired) — first live run through the publish-gated workflow.

## Files / artifacts
- `pbc_engine.py` — **self-contained pricing engine.** Reads sales JSON + collection report CSV, outputs data for pages. **COMMITTED to the repo 2026-07-31** (verified: compiles clean + runs end-to-end before commit; the Messi Gold /10 $50k and Mbappe Base Black/Nebula /1 $60k overrides are now version-controlled here, ending the loose-file/stale-copy risk). (was previously loose-file, re-uploaded each session — that caused stale-copy risk). Run: `python3 pbc_engine.py <sales_db.json> <report.csv> <base_packs> <fotl_packs> <offers.json>`
- `burning_edges_sales_db.json` — all recorded marketplace sales; the one file that must carry across chats. **COMMITTED to the repo 2026-07-31** (14,848 sales; verified valid JSON before commit).
- `offers_2026-06-22.json` — stale standing-offers snapshot (optional 5th arg; drives with-offers model)
- `PBC_DATA_CONTRACT.md` — data contract explaining engine internals + field meanings
- Pages (all live): `packs.html` (pack EV / predicted price, Customize mode), `value.html` (Value My Card w/ derivation + comps + serial tiers), `top.html` (Buyers & Sellers whale tracker), `pbc.html` (hub), `index.html`, `start.html`
- `pbc-data.js` — Netlify serverless function, live at `/api/pbc-data`

## Pricing engine logic (preserve this)
- **Pools:** silver (Base Silver) · nonsilver (Black/Gold/Blue/Red/Cracked Ice/Zebra) · insert (everything else) · fotl (Nebula/Aguila/Maple Leaf/Old Glory — FOTL-exclusive)
- **Base EV** = 2×silver + nonsilver + (0.65×nonsilver + 0.35×insert). **FOTL EV** = fotl_pool/fotl_packs + base_ev
- **Tiers:** MEGA = Messi, Yamal. ELITE = Ronaldo, Maradona, Mbappe, Haaland, Olise
- **Priority cascade:** mega base ladder (real sales) → special serials → per-copy overrides → gold /10 floor (mega $8k / elite $3k / standard = normal floor) → real sale median → premium-sale fallback (only-premium-sales: run==1 uses full value, run>1 uses premium×0.66) → rarity floors
- **Transfer filter (critical):** drop sales ≤$5, any /1 chase under $30, low-run chase under $15 — these are $1 self-transfers exploiting Panini's 7-day gift-lock. (~4,480 transfers removed in current data; 14,848 raw → ~7,937 used.)
- **Global dedup** on (athlete, cardset, serial#, price, premium) — sales files are rolling windows with heavy overlap
- **Real sales beat stale offers** for everything except mega 1/1s
- Reads `UNCLAIMED` column from collection reports (still-in-pack count). `poolCopiesTotal` is raw UNCLAIMED — no scaling. Pack counts are **typed in**, not derived from the report.
- `updated` date auto-sets to today

## Active manual adjustments (IMPORTANT — read before every run)
| Adjustment | Value | Where | Persists? |
|---|---|---|---|
| Messi Gold base /10 | $50,000 (final) | engine `GOLD_OVERRIDE` | ✅ persists (in engine) |
| Mbappe Base Black /1 | $60,000 (final) | engine `PERCOPY_OVERRIDE` | ✅ persists (in engine) |
| Mbappe Nebula /1 | $60,000 (final) | engine `PERCOPY_OVERRIDE` | ✅ persists (in engine) |
| Messi/Mbappe/Yamal blanket | +10% each run | **wrapper only, NOT engine** | ⚠️ **REVERTS on update unless reapplied** |

- The three engine overrides are **excluded from the +10% bump** (they're already final). Everything else for those three players still gets +10%.
- **OPEN QUESTION:** should the +10% be persisted into the engine as a per-player multiplier so it stops reverting? Not yet decided.

## Standing to-dos (PBC)
1. **[DONE] Step 5 wire-up** — make pages fetch from Supabase `/api/pbc-data` instead of baked-in DATA blocks, so an update = one SQL insert, no HTML paste. **DONE — merged to main and LIVE 2026-07-31 (commit 9cb8c7a). Migration run (published column added; current snapshot id=10 published, no dark window). Verified on the live deploy: packs shows Base ~$318 with overrides, value search works, both served through the `published` filter. Field contract in `PBC_DATA_CONTRACT.md`.** Verified: live endpoint returns exact shapes (payload keys <-> packs.html `DATA`; valuer `{cards,comps}` <-> value.html), 5-min CDN cache, and the served data equals the baked 2026-07-09 run (no surprise change). Approach: replace `const DATA` with `let DATA=null`, move synchronous init (cardPrices/tailPrices/cfg + first `render()`) into an `init()` run after a `fetch('/api/pbc-data?type=calc')`; value.html does the same for `cards`/`comps` via `?type=valuer`; leave calc/search/edit logic and `SPECIAL_SERIALS`/`JERSEY` untouched. Decisions (locked by John 2026-07-31): baked last-known `FALLBACK_DATA` (NOT banner-only) so a public page is never blank; a `published` boolean column + function filter (workflow: compute -> inspect -> flip `published`, preserving the review gate); branch/preview deploy only, never straight to main; PLUS a payload shape-check in `boot()` on both pages, so a future engine field rename hits the fallback instead of throwing in render(). Hard gate: engine had to be committed before merge (done, commit e6e054f). Risks to watch: async-init ordering; no review gate (any insert goes live within 5 min); output-contract coupling (engine must keep emitting these field names). *(Claude Code task.)*
2. Deploy `top.html` + `/top` route. *(Claude Code task.)*
3. Considered reframing packs page around **median** (~$22, 56% under mint) instead of **mean** — decision pending.

## Update routine (PBC)
Send new tier sales files (epic / ultra_rare / rare / uncommon / legend) + usually a collection report CSV + remaining pack counts → parse new sales, add to DB, re-run engine.

**New publish-gated flow (since Step 5, 2026-07-31 — no more HTML paste):**
1. Run `pbc_engine.py` → get `pbc_snapshot_insert.sql`; insert the snapshot. It lands `published=false`, so it is **invisible** to the live pages.
2. **Inspect** the new snapshot (numbers, overrides).
3. `update pbc_snapshots set published=true where id=<new_id>;` → live within the 5-min CDN cache. The pages fetch it automatically; nothing else to deploy.
4. Roll back by flipping the previous row back to `published=true` (or the new one to false).
5. Occasionally refresh each page's baked `FALLBACK_DATA` with a known-good payload — it's only the disaster floor, not the update path.

## GOTCHAS (PBC)
- **Comps-vs-manual-value gap:** value.html shows offer/sale comps next to assigned values. When a value is manually overridden well above visible comps, the page can look self-contradictory. Consider suppressing comps or labeling "manual valuation" on overridden cards.
- **Stale-engine risk:** if a session runs the *old* engine, the $50k/$60k overrides vanish. Mitigated 2026-07-31: the canonical engine (carrying the $50k/$60k overrides) is now committed in the repo — always run the repo copy.
- **`re.sub` escape bug:** card data contains `\u` unicode escapes; use literal string splicing (find markers, slice) to swap DATA blocks, not regex replacement.
- **Report column naming:** engine handles both `CARD SET` (with space) and `CARDSET`.

## History / investigations
- **Pack-peeking exploit (June 2026):** investigated whether PBC packs are vulnerable to peeking exploits seen in other NFT products. Determined **inapplicable** — PBC runs on Hyperledger Sawtooth (permissioned private ledger). Only residual vector is a client/API-layer leak, which would be a TOS violation. Proposed empirical correlation test (pack SKUs vs. card SKUs pulled) to check for patterns.

---

# PROJECT 2 — Moonbirds ("Birbs Beyond") — NEW, 2026-07-31

Brand-new Panini NFT drop. **First-ever Panini Moonbirds product — no secondary market history exists.** Art/PFP NFT, not sports.

## CURRENT STATE (2026-08-24) — supersedes the historical detail below
- **OFF Diamond's API — now on OUR chain engine** (same pipeline as NFL Prizm + Silhouette). Diamond drove it 08-04 to 08-24; dropped because its offers-as-floors model froze the book ~$128 through a real sales dump. Hourly `netlify/functions/moonbirds-refresh.mjs` reprices off `chain_events` (prefix `packcard-850178`, sales only) via `tools/moonbirds-reprice.mjs` + `tools/moonbirds-recompute.mjs`; the Diamond `pbc-refresh.mjs` is deleted. `pbc-api.env` now only feeds the one-time seeds.
- **Book $124.85** (predicted $312), ~1,049 packs remaining (re-seeded from Diamond's per-card counts; NOT poller-auto-decremented — see the poller carve-out below). Live at `moonbirds_snapshots` id=3.
- **Payload shape unified** with Silhouette (`pack_ev.base_breakdown/hit_breakdown`, top-level `cards_remaining`); pack = 1 Base Silver slot + 1 weighted slot2, book = base_card_ev + hit_card_ev.
- **Page rebuilt on the shared Silhouette template** — NFL/Silhouette/Moonbirds are one unified UI now. Moonbirds also feeds `/value` (reads `pack_values`).
- **Valuation** (`tools/moonbirds-reprice.mjs`): a real mid-serial sale moves value either way (dumps show through); /1 sale = the card; Birbhalla $2k global offer = hard floor; Base Silver commons capped vs washes. Seed references (Kaboom Green $20k, Base Black $2,300, Kaboom Gold $3,000) carried over from Diamond's `ordinaryValue` at cutover — open to John's QA post-dump.
- **Poller carve-out:** Moonbirds (+ Bad Eggs, untracked) can bridge cards to Ethereum, which flows $0 from the pack-vault wallet and would fake pack-opens — so Moonbirds is excluded from auto-decrement (`BRIDGE_PREFIXES` in `blockPulls`); counts kept accurate by re-seed. OPEN TODO: pin one ETH-bridge tx signature to exclude it precisely, then Moonbirds auto-tracks too. (The `blockPulls` vault-wallet fix itself DID fix count-tracking for all NON-bridge packs.)

See the 2026-08-24 changelog entry for the full migration + poller-fix detail.

---

## Status: LIVE at /moonbirds (published 2026-07-31, `moonbirds_snapshots` id=1) — HISTORICAL
Was tracking-only; now a public pack-pricing page at **/moonbirds** (`moonbirds.html`), modeled on `packs.html`. Architecture = **Option A** (same as PBC Step 5): the page fetches `/api/moonbirds-data` (function `moonbirds-data.js`) which serves the newest **published** row of a new **`moonbirds_snapshots`** table (publish gate), with a baked `FALLBACK_DATA` disaster floor + `boot()` shape-check. Data lives in `moonbirds_pricing.json` (the baked fallback AND the payload inserted to Supabase). Table DDL: `migrations/002_moonbirds_snapshots.sql` (John runs once; Claude cannot run DDL over the service key). NOT merged to main yet. Still NOT run through `pbc_engine.py` (different pools/tiers/logic).

## Set structure (from checklist `2026_Panini_NFT_Moonbirds_-_Birbs_Beyond.csv`)
- **25 characters** (Kenji, Ferno, Merlin, Enlightened Professor, etc.) — not athletes
- **133 cards total**
- Parallels & print runs:
  - Base: Black /1 · Holo Gold /10 · Purple /25 · Red /49 · Blue /99 · Silver /194 (20 cards each)
  - Kaboom inserts: Green /1 · Gold /10 · base /25 (4 cards each)
  - **Birbhalla /49** — the grail; single card (Enlightened Professor), 49 copies

## Pricing: now first-day-sales-informed (2026-07-31)
Was offers-only. On 2026-07-31, **41 first-day sales** drove a repricing (full values in `moonbirds_pricing.json`). Key moves: Holo Gold /10 floor $50 -> $300 (serial #2 sold $250 & $500); Kaboom /25 floor $250 -> $500 (one $1k mid-serial sale, thin); Birbhalla floor $1,000 -> $450 (ordinary sold $230-499); special serials up (Silver #1 $180, Blue #99 perfect-mint $250, Purple #1 $150). **Kaboom Gold #1 kept at $2,000 per John despite a $1,000 sale** (clears-above-offers). Then repriced again from **~98 sales + the collection report** (real remaining per-parallel counts -> exact current pull odds): Kaboom floor -> $800 (6 sales med $900), Red/Purple/Holo floors up, **Base Black now a real $1,669 1/1 sale** (market-anchored), Blue/Holo #1 to their actual sales. Pack model: 2 cards/pack, mint $50; **pack book EV = $119.95 (2.40x mint)**, predicted ~$300 @2.5x (borrowed multiplier, labeled speculative, not a market figure). Packs left ~3,048. Only **Kaboom Green /1** remains a pure estimate. Published as `moonbirds_snapshots` id=1 via the service-key path (`tools/pbc-db`-style inline insert + publish).

**Valuation principle (John, market expert):** cards clear ABOVE offers; scarce serials priced above the visible book on a fresh drop.

**Market-anchored now:** 8 of 10 tiers have real 7-31 sales/offers; only **Base Black /1** and **Kaboom Green /1** are pure estimates. Caveat still holds: fresh product, scarce-serial premiums are directional -> revisit as more sales accumulate.

## Values (full board in `moonbirds_pricing.json` -> tier_rules + priced_cards)
Per-parallel floor + #1/last-serial breakouts, expanded on the page. Birbhalla: #1 $10,000 / floor $450 / #49 $5,000. Kaboom Gold: #1 $2,000 / floor $1,000 / #10 $2,000. Top slot-2 EV contributors: Holo Gold ($18/pack), Kaboom ($14), Kaboom Gold ($12), Base Black ($10), Birbhalla ($9).

## Still needed for Moonbirds
- **Remaining per-parallel pack counts** (John pulling) -> refine pull odds + show "packs left".
- Real sales/offers for **Base Black /1** and **Kaboom Green /1** (still pure estimates).
- Run `migrations/002_moonbirds_snapshots.sql`, then Claude inserts + publishes the payload, then merge the branch.

---

# PROJECT 3 — MLB DFS

## Data-loading infrastructure rebuild - DONE and live (verified against code 2026-07-31)
NOTE: the MLB loaders live in a SEPARATE repo, `zoltar24601/edge-dfs-loader` (GitHub Actions -> Supabase), NOT in burning-edges-site. The app UI is `mlb.html` in this repo.

- **Daily pipeline** (`.github/workflows/daily-loader.yml`, cron 6am UTC): `results-backfill.js` -> `hitter-loader-v3.js` -> `pitcher-loader-v4.js` -> `catcher-loader.js`.
- **Hitter side:** `hitter-loader-v3.js` is CURRENT (~10 min). Ingests missing dates into `edge_statcast_daily` (1 bulk Savant CSV per date, self-healing from the max stored game_date), then computes pitch splits (2025+2026), L7/L14/L28/season windows, hot score, emerging/cooling flags FROM the table. Writes `edge_matchup_cache` (vsR/vsL) + `edge_hot_history`. Replaced the retired 90-min per-player crawler `hitter-loader.js` (kept as rollback only).
- **Pitcher side (was entirely missing from this log):** `pitcher-loader-v4.js` is CURRENT, in the daily pipeline since 2026-07-10 (~3 min vs the old 66). Incremental on the twin table `edge_statcast_pitch_daily`; writes `edge_pitcher_cache`. Replaced retired `pitcher-loader.js` (rollback only). v4 arsenals are regular-season only by design (old loader included spring/postseason).
- **Tables:** `edge_statcast_daily` (hitter) + `edge_statcast_pitch_daily` (pitcher twin; adds velo_sum/velo_n). One row per player/pitcher + game_date + hand/side + pitch_type. **Raw COUNTS and SUMS only, never averages** -> any window/split rebuilds exactly by summing rows; rates computed at read time. Backfilled 2025-03 -> present, **regular season only (hfGT=R)**.
- **Schema-v2 columns (verified present in loader code):** singles, doubles, triples, home_runs, hbp, la_sum, la_n, fb_bip, bip_typed, zone_swing -> outcome wOBA, average launch angle, fly-ball %.
- **Backfill scripts (complete, not "in progress"):** `statcast-backfill.js` (hitter) and `pitcher-statcast-backfill.js` (pitcher) - date-range, one bulk Savant CSV per date, idempotent upserts, safe to re-run any range.
- **Architecture rules (from the loaders):** counts-not-averages; bulk-per-date, never per-player (per-player crawls caused the old 60-90 min runs); idempotent upserts with explicit on_conflict; per-item try/catch so one player/date failure never kills a run; self-healing ingest.
- **Supporting loaders:** `catcher-loader.js`, `results-backfill.js`, `park-factors-loader.js`, `fangraphs-loader.py`, `clear-hitters.js`, `dump-pitcher-cache.js`.
- **Manual-dispatch workflows:** `hitter-loader-v3.yml`, `pitcher-loader-v4.yml`, `statcast-backfill.yml`, `pitcher-backfill.yml`. Secrets: SUPABASE_URL, SUPABASE_KEY (service role).
- **GOTCHA:** GitHub disables scheduled workflows after 60 days of repo inactivity - if daily runs stop, re-enable in the Actions tab.

## Standing to-dos (MLB)
- **Branch-and-bound pruning** for the MLB optimizer in `mlb.html` - logged as designed but not yet built. (Could not verify in code; carried over as a planning item.)

## History
- Origins (early 2026): first Burning Edges tool - baseball DFS matchup tool (weighted wOBA by pitch type + hot-streak scorer). Established the core stack (vanilla HTML/CSS/JS + Supabase + Netlify) used by all later projects.

---

# PROJECT 4 — Golf DFS

## Tools (all in THIS repo: `golf.html` + `netlify/functions/` + `uploads.html`)
- **Cut-survival public page (`golf.html`)** - the live public tool (the "cut-probability page" and "cut-survival tool" in the old log are the same page). 50/50 side-by-side desktop layout (`.split` flexbox, two equal result cards: field distribution + "Find My Lineup"); stacks vertically at the 880px breakpoint. Two DataGolf-driven modes: **LIVE** = Poisson-binomial survivor distribution over DataGolf make-cut odds, averaged across all contest teams; **FINAL** = actual survivor count per team after the 36-hole cut. First run was RBC Canadian Open (June 2026); the page is generic/live per event.
- **"Find My Lineup"** - enter a DK username; ranks all that user's lineups by expected survivors, with clean-sweep / wipeout %.
- **Netlify functions:**
  - `golf-cut.js` (/api/golf-cut) - field cut-survival distribution per contest (up to 3) vs shared DataGolf make-cut odds; 30-min throttle so public traffic never hits DataGolf's 45 req/min limit.
  - `golf-field.js` (/api/golf-field) - raw DataGolf in-play field feed (positions, scores, cut/win/top-N odds); module-scope cache; deliberately NOT cached in `golf_distribution`.
  - `golf-lookup.js` (/api/golf-lookup?user=) - finds a DK username's entries (case-insensitive exact match) in the stored event, computes each lineup's cut-survival odds, returns them ranked by expected survivors. Fire-and-forget analytics log to `golf_lookups` (never blocks the user's result).
  - `golf-upload.js` (/api/golf-upload, POST, private) - accepts a gzipped, dictionary-encoded payload (handles 100K+ lineups); stores the compact encoded form (dict + index arrays) in Supabase; `gunzipSync`.

## Technical notes / gotchas (Golf)
- **Large-upload handling (`uploads.html` + `golf-upload.js`):** browser-side dictionary-encode + gzip via `CompressionStream` before POST, to stay under Netlify's ~6MB function request limit; the server detects gzip by magic bytes `0x1f 0x8b` (not headers) then `gunzipSync`s. (This is the "U.S. Open upload fix"; originally cut a ~16MB payload to <1MB.)
- **Lineup parsing (`uploads.html`):** `splitLineup()` splits a DK lineup string on the `G ` position markers (regex `/(?:^|\s)G\s/`), trims, and drops blanks.
- **CORRECTION (2026-07-31):** the previously logged `utf-8-sig` / `abbr()` / manual "F. Last" fix-dict parsing conventions are NOT present anywhere in the committed repo. Current name-matching (`golf-lookup.js`) is case-insensitive on stored lineup names vs DataGolf `player_name`. If those Python-side conventions still exist, they live in an uncommitted prep step, not this repo.
- `golf_lookups` analytics table is written fire-and-forget by `golf-lookup.js`. (RLS is a Supabase table-level setting, not verifiable from repo code.)

---

# PROJECT 5 — Browser automation tools (Chrome extensions)

**Outside this repo** — these live on John's Desktop (`C:\Users\johnq\OneDrive\Desktop\`), not in burning-edges-site. Logged here because this doc is the single source of truth for all Burning Edges work. Each is a Manifest V3 extension (popup + content.js + background relay) acting on `https://nft.paniniamerica.net/*`.

**Boundary rules held across all three (John + Claude agreed):**
- DOM reading is undetectable; programmatic clicks are honest automation, NOT stealth — no bot-detection evasion is built, and John was told plainly it can trip Panini's terms.
- **Won't auto-execute anything that moves money** — pack *buys* stop at checkout for John to click. **Offers/bids CAN auto-place** because placing an offer moves no money (money only moves if a seller later accepts).

## `offer_placer` (the "OG bot")
The original, John's pre-existing tool. Places N offers on ONE already-open card (`.html` card page), one shot, human moves to the next card. Proven offer flow reused by the collection bidder. Key stable selectors: `.make_offer button`, `#offer_price_id`, `#duration_btn_id` (60 days = `li:nth-child(7) > a`), payment label fallback (paypal/credit/card). Its terms checkbox `#auctionTimerExtend` is now STALE on the newer offer UI (see collection bidder).

## `pack_sniper` — DONE, working
Assist tool for pack drops. Scans a pack's Marketplace listings, opens a random pack in a configurable rank range (default 5th–15th cheapest) under a max price, ticks the terms box, and STOPS at checkout — **John clicks the final BUY IT NOW** (money-moving). "Keep going" loop after each buy; on a sold-out/can't-advance state it just refreshes and continues (John's idea — no need to detect the specific error). Pinned selectors: listing rows `tr.position-relative`, buy button `button[aria-label^="Buy now for" i]`, checkout is a Bootstrap **offcanvas** (<1s, not a page nav), final button `#pack-buy-now-btn` (never clicked by bot), terms `#tc-checkbox-crypto-purchase`, post-buy "Congratulations…" modal → reloads list. Listings page detected by URL `/marketplace-details/`; listings don't live-update (reload for fresh data). Speed is the priority (packs sell in seconds).

## `collection_bidder` — DONE, working (2026-08-22)
Walks a whole collection A→Z and auto-places a Global offer on every card: **$1, quantity 5, 60-day, PayPal** (all tunable in the popup: price / qty / burns-left cutoff). Auto-places fully (offers don't move money). Two things new vs the OG bot: the **quantity step** (bulk bids didn't exist before) and **card-to-card auto-advance** (OG was one-shot). Working end-to-end and fast.
- **Burn-skip (John's rule):** cards get burned toward 50% of population, at which point the card is useless. Skip if `(total / 2) − burned ≤ 30` burns remaining (total = burned + applicable = the `#/N` denominator). Reads it off the card (`#/2000` + `BURNED nnn`) or the offer panel (`APPLICABLE FOR X CARDS … X CARDS WERE BURNED`). Aaron Gordon 927/2000 → 73 left → bids; ≥970 burned → skips.
- **Hard-won DOM lessons (took many rounds — captured so we never re-derive):** the clickable grid card is **`a.nft_link_wrapper`** — navigate by its `href` to `marketplace-details/packcard-*.html`. NOT `.nft_items` (those image tiles live in a hidden offcanvas; the first `.nft_items` is just the Prizm logo banner). Make-offer via `.make_offer button` (text "MAKE AN OFFER" broke substring matching). Quantity "+" = `button[aria-label^="Increase offer quantity"]`, clicked until its aria-label reads "current quantity 5" (blind counting dropped a click → landed on 4). Duration by "60 Days" text. Payment = label containing "CREDIT CARD/PAYPAL". Terms = first visible `input[type=checkbox]` on the confirm screen (the OG's `#auctionTimerExtend` id is gone); confirm button stays disabled until it's ticked. **Ordering:** the grid is virtualized + browser restores scroll on return → set `history.scrollRestoration='manual'` and scroll to top before each scan, else it teleports to random cards (Zion Williamson). Full selector list in the folder's `README.txt`.

---

# Cross-project standing decisions
- **Documentation practice (2026-07-31):** maintain this master log as work happens, committed to the repo as canonical, to make chat handoffs clean. Pricing decisions from chat + backend changes from Claude Code both land here.
- **Engine-in-repo (DONE 2026-07-31):** `pbc_engine.py` and `burning_edges_sales_db.json` are committed in the repo, ending the loose-file re-upload / stale-copy risk. The $50k/$60k overrides are version-controlled. Engine was verified (compiles + runs end-to-end) before committing.

---

## Changelog
- **2026-08-06 (Silhouette remaining now CHAIN-LIVE — manual CSV ended)** — Wired the Silhouette page's "remaining" to the chain-tracked `card_remaining` table (poller-maintained) instead of the manual report. Approach: froze the owner-model per-card values in `tools/silhouette-values.json` (Flagg 1/1 = $40k), and `tools/silhouette-recompute.mjs` rebuilds the payload from live `card_remaining` counts + those frozen values (prices stay put, only counts move). `netlify/functions/silhouette-refresh.mjs` runs this `@hourly` (needs only the existing SUPABASE env). First chain-sourced snapshot published: book $103.59 (identical to the report seed — validates the recompute). As packs are ripped, Silhouette remaining now ticks down on its own. SOCCER still on manual report — same pattern applies but needs the pbc_engine value model ported to a frozen map (bigger job, next). Moonbirds already auto-updates via Diamond's feed.
- **2026-08-06 (Pull tracker BUILT — derives 'remaining in packs' from chain)** — The CSV-killer. `/state` is NOT exposed by explorerapi (404), so remaining can't be snapshotted directly; instead: **seed `card_remaining` ONCE from a collection report (the last manual CSV), then the poller decrements it per on-chain pull.** Built: `migrations/006` (`card_remaining` seeded from report UNCLAIMED + `chain_pulls` serial-level table + `decrement_remaining()` RPC); `tools/seed-remaining.mjs` (one-time seeder: `node tools/seed-remaining.mjs <report.csv> <product>`); `panini-chain.mjs` **`blockPulls()`** — pull-detection heuristic: a **`$0 transfer_product` (run<=500) in a block that also contains a `burn_product`** = a real pack rip (burn = the pack consumed; the $0 card transfers are its contents to the opener). This cleanly excludes $0 consolidations/gifts/mints (which occur in non-burn blocks). VALIDATED: 7 burn-blocks -> 9 pulls, each with exact serial + opener wallet. `burn_product` payload = `{action, date, product_sku_id (the pack, run in 1000s), to_customer_pub_key (burn/system wallet)}`. Poller now records `chain_pulls` (dedup on tx_id) + calls `decrement_remaining` per NEW pull each tick. DONE: migration 006 run; `card_remaining` SEEDED (Moonbirds 133/3241, Silhouette 1736/11488, Soccer 7170/42923). LOOP VALIDATED LIVE 2026-08-06: 3 soccer pulls (block 4211645) auto-decremented card_remaining (Nico O'Reilly Base Blue /49 5->4, David Alaba 7->6, Diogo Dalot 7->6) + logged serials #46/#39/#16 to chain_pulls; chain sku_base joined perfectly to report PSKU (real player names). POLLER LIVE 2026-08-06: deployed to Railway (project spectacular-analysis, service burning-edges-site, root dir `chain-poller`, env SUPABASE_URL + SUPABASE_SERVICE_KEY). Build needed a **Dockerfile** (`chain-poller/Dockerfile` FROM `mcr.microsoft.com/playwright:v1.47.0-jammy`) because Railway's Nixpacks image lacks Chromium's system libs (libglib etc) — plain playwright install downloaded the browser but it couldn't launch. With the Playwright image it runs clean. **It passes Cloudflare from Railway's datacenter IP fine — no residential proxy needed.** Confirmed indexing autonomously: cursor advancing, chain_events + chain_pulls growing every ~30s. REMAINING STEP: repoint Moonbirds/Silhouette pricing to read `card_remaining` instead of manual CSV uploads (Claude to build). NOTE: detection may need edge-case tuning (validate derived-remaining vs a fresh report once running). Diamond openly invited hooking into his pipeline; future Panini bridge off Sawtooth is a shelf-life risk -> keep decoder/pricing layer portable.
- **2026-08-06 (Silhouette Flagg /10 -> $7,500; /changes now multi-pack; pack-pull roadmap)** — Per John + on-chain validation: bumped **Cooper Flagg /10 inserts $4,200 -> $7,500** (real chain sale RRR Blue #7/10 = $7,500; formula estimate was light). **Flagg 1/1 stays $40,000 — explicitly untouched.** Republished silhouette_snapshots id=1, book $102.39 -> **$103.59**. Extended the public **`/changes`** feed to be multi-pack (product tag per row: Moonbirds purple / Silhouette flame) and logged the Flagg /10 move under product=silhouette. BIG ROADMAP (John's realization): since the chain records every pack pull WITH exact serial, indexing pulls lets us (a) **derive "remaining in packs" from the chain -> no more manual collection-report CSV uploads**, and (b) **know which exact serials are pulled vs sealed -> serial-level "Hits Remaining"** (e.g. "Birbhalla #1 still in packs") + more accurate pricing. On-chain actions found: `transfer_product` (price>0 = sale; price=0 = pull/gift/mint), `burn_product` (pack consumed on open), `lock_product` (7-day gift lock), `create_customer`. A pack rip = a `burn_product` + several $0 card transfers to a USER wallet; but $0 transfers also include Panini SYSTEM bulk-mint (25-42 same-card copies to system wallets) so real rips need filtering. NEXT to investigate: the Sawtooth **`/state`** endpoint (live ownership snapshot = what the collection report is generated from) to read "remaining" directly (CF hiccup on first try, retry).
- **2026-08-06 (Chain indexer PROVEN end-to-end on Silhouette + live ingest)** — Loaded 80 live chain events into Supabase `chain_events` (all products in one feed — incl a Moonbirds sale `packcard-850178...`, unlike Diamond's soccer-only tracker). Then tested on Silhouette: SKU prefixes are **`packcard-2217`** (base set) + **`packcard-2305`** (inserts/Instant); built `tools/silhouette-skumap.json` (1,736 PSKU->{athlete,cardset,run} from the report). The explorer API caps `/blocks` at 20/call but pages via `paging.next` (`?head=...&start=0x...&limit=20`) — walked back ~980 blocks (~1 day), pulled **15 real Silhouette sales**, joined **12/12** to card names incl **Cooper Flagg Retro Rated Rookie Blue #7/10 = $7,500**, VJ Edgecombe RPS #11/49 $100, etc. So our OWN chain read prices Silhouette (and any pack) with zero Diamond dependency. Diamond's post ("SawtoothExplore(r) v.04") confirms the approach, notes his tracker is SOCCER-ONLY, OPENLY invites builders to hook in ("reach out"), and flags a **future bridge** off Sawtooth (don't over-invest in deep Sawtooth infra; keep the decoder/pricing layer portable). NEXT: poller accumulates sales over time -> wire `chain_events` (filtered by product prefix) into the Silhouette + Moonbirds pricing engines.
- **2026-08-06 (NEW: independent Panini Blockchain indexer — in progress)** — Goal: our OWN live sales feed off the Panini chain (Hyperledger Sawtooth), instead of depending on Diamond's `/birbs` API. Diamond endorsed the direct-monitor approach. FINDINGS: (1) Data source = the Sawtooth explorer's REST/WS API at **`https://explorerapi.paniniamerica.net`** (endpoints `/blocks?limit=N`, `/transactions/{id}`, `/state`, live `wss://.../subscriptions`). (2) Transaction family = **`panini-cx-crypto` v1.0**; each txn `payload` is **base64-encoded JSON**: `{action, date(ms), price, product_sku_id, from_customer_pub_key, to_customer_pub_key}` — trivial to decode (NOT protobuf/CBOR). `action:"transfer_product"` + `price>0` = a sale. `product_sku_id` e.g. `packcard-1940_377840_9774508_1__42_49` = `<base>__<serial>_<run>`; the `<base>` matches the collection-report PSKU -> card name/parallel/run. (3) **Cloudflare** fronts the API: a plain server `fetch`/curl gets **403**, but a real browser passes the managed challenge (verified — pulled live block 4211629, decoded its sales). BUILT + committed: `tools/panini-chain.mjs` (decoder, tested on real txns), `migrations/005_panini_chain.sql` (`chain_events` + `chain_sync` cursor), `chain-poller/` (always-on Playwright worker: real browser -> passes CF -> polls `/blocks` -> decodes -> upserts `chain_events` -> advances cursor; self-contained w/ vendored decoder + package.json + README deploy steps). John approved an always-on host (~$5/mo, Railway); no Diamond node yet (so headless-browser path). PENDING: John runs migration 005; deploy `chain-poller` to Railway (root dir `chain-poller`, env SUPABASE_URL + SUPABASE_SERVICE_KEY); then build sku_base->card metadata join + wire chain feed into the pricing engine. RISK: Cloudflare may challenge harder on a datacenter IP than an interactive browser -> may need a residential proxy or Diamond's peered node (`localhost:8008`, CF-free) as the sturdier route. See [[moonbirds-live-pipeline]].
- **2026-08-04 (Moonbirds valuation: serial-aware ordinary)** — Per John (Kapow Kaboom Gold $7k was serial #1, not ordinary): `tools/pbc-value.mjs` now uses the API's per-sale `start_seq` to split sales into #1/last-serial (PREMIUM) vs mid-serial (ORDINARY). Ordinary value = mid-serial sales only; a #1/last sale (already sold, so not sealed) never inflates it. `stackValue` now values EVERY sealed copy at ordinary — no longer bakes an already-sold grail into the pull EV. #1/last premiums still show on the tier board / serial lookup as reference. Added `Kaboom Gold` $3,000 reference (Kapow lands there). Pack book **$147.81 -> $117.63** (Kaboom Gold parallel $3,546 -> $2,108; Birbhalla $1,088 -> $700 since its low serials already sold). This is the codified hourly-pipeline logic, so it self-applies. Also: soccer engine `pbc_engine.py` fixed — added Cristiano Ronaldo Base Prizms Black /1 to PERCOPY_OVERRIDE ($40k) so it stops dropping out of top-120.
- **2026-08-04 (Fixes: Silhouette Flagg 1/1 $40k, Soccer Ronaldo Black restore)** — Silhouette: per community feedback, Cooper Flagg 1/1 pinned to **$40,000** (override in the build for `Cooper Flagg` run<=1). Only his NBA Draft Night Black /1 is still sealed (rest claimed); book $94.42 -> **$102.39**. Republished silhouette_snapshots id=1 + synced fallback. Soccer: the live pbc_snapshots (2026-07-31 engine run) had **dropped Cristiano Ronaldo Base Prizms Black /1** entirely — restored it to the published payload (`top`), then set to **$40,000** (has a $30k offer; clears above offer), now shows in Hits Remaining. NOTE: this is a live-snapshot patch — the next soccer `pbc_engine` run + republish will drop it again unless the source is fixed; and his **National Landmarks Prizms Black /1 ($12k)** is ALSO missing from the live data (not yet restored). Root cause of the 7-31 drop not investigated (would need that report/engine).
- **2026-08-04 (Moonbirds LIVE-DATA pipeline + public /changes feed)** — Cut Moonbirds over to the **Panini Blockchain Tracker** API (`/api/birbs`, x-api-key; key in `~/.burning-edges/pbc-api.env`, also needs adding to Netlify env as `PBC_API_KEY`). Built by Diamond (Discord) / @diamondNFL (X) — credited on the page. VALUATION RULES (`tools/pbc-value.mjs`, per John): an **offer is a FLOOR, never discounted**; manual references hold until a **real sale** moves them — `Kaboom Green $20,000`, `Base Black $2,300`; a scarce-serial premium prices only the ONE best remaining serial (a single big offer doesn't reprice all copies). Payload builder `tools/pbc-build-moonbirds.mjs` maps the API into the existing page payload (slot2 breakdown, tier board, cards_remaining, chase strip) and detects major moves. HOURLY automation: `netlify/functions/pbc-refresh.mjs` (`schedule:@hourly`) pulls the API -> rebuilds -> PATCHes the published `moonbirds_snapshots` row -> inserts major moves into `price_changes`. Public feed: `/changes` page + `changes-data.js` function reading `price_changes` (migration 004). First live-data snapshot published (id=2) = book **$147.81** (matches the hand-tuned $146; greens correctly $20k). PENDING JOHN: (1) run `migrations/004_price_changes.sql` in Supabase SQL editor; (2) add `PBC_API_KEY` to Netlify env vars -> then the hourly function runs autonomously. Silhouette stays manual (no API yet).
- **2026-08-04 (NEW PACK: Silhouette Basketball, book $94.42)** — Built a second Panini pack analytics page, `/silhouette`, same pipeline as Moonbirds. Product: 2024-25 Panini NFT Silhouette Basketball. Pack model: **4 cards = 3 base-pool + 1 insert/Instant hit**. Base pool (8,727 unclaimed) = Base /256 + parallels; hit pool (2,761) = every insert (White Night, Vanta, Contour, Superstars, Hand Crafted, Pillars, Game 7, Kings Court) + Panini Instant set (Draft Night, Retro Rated Rookie, RPS First Look, Spotlight Rookies, Year One). Pricing is **player-tier x set x parallel** (hit value is entirely player-driven): S=Cooper Flagg, A=Harper/Edgecombe/Knueppel, B=notable rookies, V=vet stars, C=common; hit value = recent-sale median where available (from `recent sales.txt`), else tier x parallel anchor; base slots = parallel floors. Pack book EV **$94.42** (base $11.81 + hit $82.60), predicted ~$236. Running sales log `silhouette_sales_log.json` (527 de-duped sales from recent + top files). New files: `silhouette.html` (baked fallback, hits/chase/pull-odds sections, flame accent), `netlify/functions/silhouette-data.js`, `migrations/003_silhouette_snapshots.sql`, `/silhouette` + `/api/silhouette-data` in netlify.toml, Silhouette card added to `packs-hub.html` (order:-1, alongside Moonbirds). LIVE: John ran migrations/003; Claude inserted+published `silhouette_snapshots` id=1 (published=true) 2026-08-04 — page is now DB-backed. FIRST-PASS pricing — anchors (esp. Cooper Flagg tiers, A-tier rookies) are open for John's QA once live.
- **2026-08-04 (Moonbirds report(4) + reality reprice, book $146.03; log 858)** — Logged sales dump `Part 6.txt` (+445 new after dropping 253 dupes -> running log **858**). Report (4): **1,617 packs** left (~730 opened since report 3 — opening accelerated). Reality-anchored reprice off real sales: **Kaboom Green $10k -> $25,000** (Kapow 1/1 sold $25,035 ETH; only 2 left sealed - Pix, Fizz); **Kaboom Gold #1 $2,000 -> $6,000** (Kapow #1 sold $5,555 & $6,999), floor holds $1,600, #10 $2,400; **Base Holo Gold #1 $750 -> $1,300** (Nova #1 $1,291), **#10 $750 -> $500** (Noct perfect $500), floor $300; **Base Silver #1 $165 -> $215** (cluster $195-300, lifts slot1 avg to $13.49). **Birbhalla #1 (the /49 grail at $10k) PULLED** per John - removed from Hits Remaining + chase strip (kept as a $10k valuation in the tier board for Price-My-Card). NOTE for next time: Birbhalla low serials #3/#9 sold ~$2,000 (ETH) vs $600-700 for mid/high - possible low-serial premium to model. Pack book EV $136.27 -> **$146.03** (predicted ~$365, 2.92x). Published to live id=2.
- **2026-08-01 (Moonbirds report(3) counts, book $136.27; sales log 413)** — Logged sales dump `part 5.txt` (+176 new after dropping 69 dupes -> running log **413** sales). Refreshed remaining counts from collection report (3): **2,346 packs** left (John's count; slot-2 pool 2,385), ~330 more opened since report (2). Republished to live `moonbirds_snapshots id=2` at CURRENT floors: book EV **$136.27** (predicted ~$341, 2.73x), 67 hit rows. TRENDS flagged (floor calls still OPEN with John): **Kaboom Gold underpriced** — floor $1,250 but every sale $1,500-2,424 (perfect #10 $2,424), supply 28->21; **regular Kaboom splitting** — Kapow holds $1,050-1,100 but Fizz/Pix ordinaries $300-600 vs our $900 floor; **Holo Gold** ordinaries $150-300 (Merlin 8/10 $799 outlier) vs $375 floor; Birbhalla ordinaries $600-700. John approved all four (same day): **Kaboom Gold floor $1,600 / #10 $2,400 / #1 $2,000**, **Kaboom floor $900->$600** (Kapow the exception), **Holo Gold $375->$300**, **Birbhalla $700->$650**. Applied + published to id=2: book EV **$136.27 -> $128.37** (net DOWN because Kaboom (55 copies) and Holo Gold (119 copies) floors fell more than the Kaboom Gold (21 copies) bump lifted). tier_rules/chase_serials/priced_cards all synced.
- **2026-07-31 (Moonbirds report(2) + PM reprice, book $134.05)** — Refreshed remaining counts from collection report (2): 2,715 packs left, slot-2 pool 2,755. Logged sales dump `part 4.txt` into the running log (+88 -> 237 sales). Reprice calls from the new sales: Base Holo Gold floor $400 -> **$375** with #1/#10 top-serial premium **$750** (first mid-serial sale #8 hit $150, but John values top serials up); Birbhalla ordinary $500 -> **$700** (ordinaries cluster there); Kaboom Gold floor $1,000 -> **$1,250** (mid #5 sold $1,500); Base Purple #1 premium $150 -> $200. Also earlier this session: Kaboom Green /1 $5k -> **$10k** ($15k open offer) and Birbhalla **#1** broken out as a $10k grail hit. Pack book EV $127.27 -> **$134.05** (predicted ~$335, 2.68x). Published to live `moonbirds_snapshots id=2`; fallback synced. OPEN: John asked to "check offers" — no offers data in the sales/report files; awaiting an offers export.
- **2026-07-31 (Moonbirds LIVE $127.27 + Hits Remaining)** — Published `moonbirds_snapshots` id=2 (id=1 retired): pack EV $127.27 (2.53x mint), 2,898 packs left, pull odds from the corrected collection report's real remaining counts. Added a soccer-style **Hits Remaining** section to the page: 68 scarce cards (/25-and-rarer + the Birbhalla grail) with remaining counts + estimated values, plus a chase-serial premium strip (report gives counts, not which exact serials remain; section auto-hides if a snapshot lacks the data). Running de-duped sales log `moonbirds_sales_log.json` (149 sales) is the pricing basis.
- **2026-07-31 (Moonbirds running sales log + reprice)** — Started `moonbirds_sales_log.json` at the repo root: a running, de-duped sales log (dedupe on character|parallel|serial|price; append new export files to it) so we keep the whole history, 149 sales so far. Repriced Moonbirds off it: Base Black $1,669 -> $2,300 (2nd 1/1 sale at $3k), Kaboom floor -> $900, Kaboom Gold #10 -> $1,500 (real perfect-mint sale), Silver #194 -> $99, Blue/Purple/Birbhalla tweaks. Pack EV $119.95 -> $126.64. Staged as `moonbirds_snapshots id=2` (published=false); packs left ~2929. (Soccer report #16 was attached but the sales were all Moonbirds - soccer not repriced.)
- **2026-07-31 (Moonbirds LIVE + pack hub)** — Moonbirds page live at /moonbirds: repriced from ~98 first-day sales + the collection report's real remaining counts (pack book EV $102.51 -> $119.95, 2.40x mint; Base Black now a real $1,669 sale), published as `moonbirds_snapshots` id=1 via the service-key path, branch merged. Also shipped **/packs** - a pack-listing hub (Soccer Base, Soccer FOTL, Moonbirds cards, each pulling live book/predicted from /api/pbc-data + /api/moonbirds-data and linking to its page). Fixed a /packs route collision with packs.html via `force = true`, and set predicted-price-first on the hub + Moonbirds price band.
- **2026-07-31 (Moonbirds page built)** — Built the Moonbirds pack-pricing page at /moonbirds on branch `moonbirds-page`, Option A architecture (new `moonbirds_snapshots` table + `/api/moonbirds-data` function + published gate + baked fallback + boot() shape-check), a sibling of packs.html. Repriced from 41 first-day sales: pack book EV $102.51 (2.05x mint); Holo Gold/Kaboom floors up, Birbhalla floor down, Kaboom Gold #1 held at $2k per John. QA passed (EV display, Kaboom Gold/Birbhalla tier expansion, 8 market / 2 estimate badges, fallback render). NOT merged - pending John running `migrations/002_moonbirds_snapshots.sql` + reviewing the preview, then Claude inserts+publishes.
- **2026-07-31 (direct DB writes enabled)** — Claude can now read+write `pbc_snapshots` directly via the service-role key, through `tools/pbc-db.mjs` (key lives only in `~/.burning-edges/supabase.env`, gitignored/outside repo, never printed). MCP OAuth write path abandoned — removing `read_only` from the URL just broke the MCP connection (needs re-auth; same wall as 2026-07-10). Future pricing publishes are Claude end-to-end: engine → `pbc-db.mjs insert <calc> <valuer>` → show numbers → `pbc-db.mjs publish <id>`. No more SQL paste.
- **2026-07-31 (pricing run — report #15)** — First live pricing update via the new publish-gated workflow. Re-priced on report #15 (White Sparkle rows excluded — separate pack), pack counts 7,217 base / 2,141 FOTL. Clean canonical engine (no offers, no +10% bump — John's call). Base book $88.96 → ~$222, FOTL $139.43 → ~$349 (down from $127/$318: ~40% of packs opened + the methodology change). Inserted as `pbc_snapshots` id=11 (`published=false`) → verified shape/numbers → flipped to `published=true`, id=10 retired. Verified live on packs.html (base ~$222, updated 2026-07-31).
- **2026-07-31 (Step 5 LIVE)** — Merged `step5-supabase-wireup` → main (commit 9cb8c7a); packs.html/value.html now serve from Supabase via the `published`-filtered `/api/pbc-data`, with a baked `FALLBACK_DATA` disaster floor + boot() shape-check. Ran migrations/001 (published column added; current snapshot id=10 published — no dark window). Verified on the live deploy: packs Base ~$318 with overrides, value search works, both through the filter. Update routine is now publish-gated (insert `published=false` → inspect → flip to true; no HTML paste).
- **2026-07-31 (Step 5 built on branch)** — Implemented the Supabase wire-up on branch `step5-supabase-wireup` (NOT merged). packs.html/value.html now fetch `/api/pbc-data` and fall back to a baked `FALLBACK_DATA` on fetch/shape-check failure; `boot()` validates payload shape first. Function filters `published=eq.true`; added `migrations/001_pbc_published.sql` (publish gate) and `PBC_DATA_CONTRACT.md` (field contract). QA passed: live==baked numbers, both pages render from fetch, forced failure falls back cleanly. Pending: John's diff review → run migration → merge.
- **2026-07-31 (engine committed)** — Added `pbc_engine.py` (canonical, with Messi Gold /10 $50k + Mbappe Base Black/Nebula /1 $60k overrides) and `burning_edges_sales_db.json` (14,848 sales) to the repo. Verified before commit: engine compiles clean and runs end-to-end (BASE ~$92 book), overrides confirmed in output. Flipped all engine-location + sales-DB lines from PENDING to committed; the earlier PENDING correction is now resolved.
- **2026-07-31 (Step 5 plan)** — Reviewed `pbc-data.js` + `packs.html` + `value.html` + the `pbc_snapshots` schema for the Supabase wire-up. Confirmed stored `payload`/`valuer` shapes exactly match the pages and the live endpoint serves them (5-min cache). Plan approved and recorded in PBC standing to-dos; implementation deferred (not urgent). No code changed.
- **2026-07-31 (log review)** — Reviewed MLB + Golf sections against actual code. MLB: rebuild is DONE and live (daily-loader.yml runs hitter-loader-v3 + pitcher-loader-v4); documented the pitcher-side rebuild (pitcher-loader-v4 + edge_statcast_pitch_daily twin table) that was missing; noted loaders live in the separate edge-dfs-loader repo; verified schema-v2 columns. Golf: confirmed golf.html cut-survival page + 4 Netlify functions (golf-cut/field/lookup/upload); corrected the utf-8-sig/abbr/fix-dict "parsing conventions" (not present in the repo); documented the actual splitLineup + CompressionStream/magic-byte upload path.
- **2026-07-31 (correction)** — Corrected engine-location claims: `pbc_engine.py` verified NOT in the repo and not present anywhere on the machine; `burning_edges_sales_db.json` exists only in Downloads. Changed all engine-in-repo statements to PENDING COMMIT. The earlier "engine-in-repo migration" note (same day) was premature.
- **2026-07-31** — Created master log. Started Moonbirds tracking (Birbhalla serial tiers set). Established doc-as-canonical practice. Noted engine-in-repo migration.
- **2026-07-09** — PBC pricing run: 11,950 base / 4,030 FOTL. Added Messi Gold base $50k + Mbappe Black/Nebula /1 $60k as persistent engine overrides. Applied +10% Messi/Mbappe/Yamal (output-only).
- **~2026-07-01** — Prior PBC baseline: 18,476 base / 5,365 FOTL; sales DB at 14,848; engine + data contract established.
- **2026-08-08 (NEW PACK kickoff: 2021 Panini NFT NFL Prizm)** — John: "lets price nfl prizm 2021 packs." Received the 3 inputs: collection report `Panini_Collection_Report_2021_FOOTBALL_2021 Panini NFT NFL Prizm.csv` (3,725 card rows, 27 parallel tiers) + recent-sales dump (`nfl 21 week.txt`, 320 sales parsed) + top-sales dump (`nfl 21 top.txt`, 300 sales parsed). **Chain SKU prefix = `packcard-1584`** (so the poller can auto-track pulls/remaining once seeded). Report totals: **196,837 cards still sealed (UNCLAIMED)**, 1,197,202 with collectors, 1,112,766 burned/opened. VALUE LADDER (median|max across players): Absolute Kaboom /99 $2,600|$43,000 (Brady 99/99 perfect = $43k top sale) > Gold Vinyl 1/1 $2,100|$35,012 > Gold /10 $1,650|$8,000 > Red /75 $1,250|$8,750 > White Sparkle /8 $950|$15,000 (Brady) ... base/silver/rookies commons $1-3. PLAYER APEX: Tom Brady ($43k) & Patrick Mahomes ($26k), then QB/legend tier (Herbert, Allen, Lamar, Marino, Payton, Bo Jackson, B.Sanders, Brees, Rice, Peyton, Burrow, Trevor Lawrence, JJefferson, JaMarr Chase). John ANCHOR (owner model, hard-hold like Flagg 1/1): **serial-#1 Tom Brady Gold /10 = ~$150,000** (marquee chase; note Brady Golds /10 ordinarily trade ~$5k, so this is a serial-1 GOAT premium). PENDING John inputs before pack EV: (1) pack composition (cards/pack + slot odds), (2) packs-remaining count. Parsed sales saved to scratchpad. NEXT: freeze per-card value map (player-tier x parallel, serial-aware #1/perfect premiums), seed card_remaining (product=nflprizm21, prefix 1584), wire hourly refresh + chain poller, publish /nflprizm page.
- **2026-08-08 (NFL Prizm 2021: page + pipeline BUILT, pending migration 007 + review)** — Full build mirroring Silhouette. VALUE MODEL frozen to `tools/nflprizm-values.json` (3,725-card universe): player-tier x parallel, junk-transfer + challenge-corner artifacts scrubbed (John: base/common overpays were money-transfer junk cards; Landon Collins Red = challenge-corner FARCE -> ordinary; Pat Tillman KEPT, real value). Tier scoring excludes Base/Rookies/Red/Challenge/GoldVinyl so junk/corner sales can't inflate a player's tier. OWNER OVERRIDES (John, hard-held like Flagg 1/1): **Tom Brady Gold /10 = $50,000 normal / #1 = $150,000**, **Patrick Mahomes Gold /10 = $30,000**. Pack = 5 cards (3 Base + 1 Rookie Base + 1 Parallel[5/6] or Insert[1/6]); math validated against report sealed counts (3xBase=117,687 vs 118,101; etc). **PACK BOOK EV = $23.51**, predicted ~$59, 39,367 packs left, 57 chase cards >=$300, Brady #1/10 $150k grail surfaced. FILES: `nflprizm.html` (baked fallback + fetch /api/nflprizm-data, pine/flame theme, 5th-slot pull-odds tables), `tools/nflprizm-recompute.mjs` (recompute payload from live card_remaining + frozen map; SERIAL1 overlay), `netlify/functions/nflprizm-data.js` (+ `nflprizm-refresh.mjs` @hourly), `migrations/007_nflprizm_snapshots.sql`, netlify.toml routes (/nflprizm, /api/nflprizm-data), packs-hub card (green accent). CHAIN: prefix **`packcard-1584`**; **card_remaining SEEDED** (product=nflprizm21, 3,725 rows / 196,837 sealed) so the existing poller auto-tracks pulls now. QA'd page render locally (EV/grail/pull-odds all correct). PENDING: (1) John runs migrations/007 in Supabase SQL; (2) Claude inserts nflprizm_snapshots published=false -> John reviews live preview -> publish=true; (3) commit+push to deploy. OPEN pricing calls for John: Jabrill Peppers Red (farce'd to ordinary, same corner pattern as Collins), Antonio Gates Red (kept modest as HOFer) -- confirm or adjust.
- **2026-08-08 (NFL Prizm 2021 LIVE)** — Pushed to main (commit ac61122); Netlify deployed. Published nflprizm_snapshots id=1 (published=true). Verified live: `/nflprizm` HTTP 200, `/api/nflprizm-data` serving book $23.51 / predicted $58.78 / 39,367 packs / 57 chase cards / Brady #1/10 $150k grail. Page is DB-backed; hourly nflprizm-refresh will keep remaining counts current from chain pulls (prefix packcard-1584). John confirmed: leave Jabrill Peppers (ordinary) + Antonio Gates (modest) as set. Pack is now in the /packs hub. Pricing anchors remain open to QA/adjust anytime via re-freeze + republish.
- **2026-08-11 (Pack repricing: recency engine + poller count-bug fix)** — John: prices must constantly reevaluate off SALES (last-3-months precedence over last-12), even if a price holds; owner anchors are floors. DIAGNOSIS of "are live updates working?": chain poller alive (block ~4.217M) + all 3 hourly refreshes firing, BUT (1) NFL counts frozen — `blockPulls` gated on `run<=500` silently dropped all 2021 NFL Prizm base/rookie pulls (numbered /3250-/6000); prefix 1584 confirmed correct + actively trading (most-traded product on-chain). (2) `chain_events` sales were being COLLECTED but NOT consumed to reprice values (NFL + Silhouette = frozen owner model; only Moonbirds reprices off sales, via Diamond's API). SHIPPED (commit d6b71fe): **(a)** recency reprice of NFL off the dated sales history — 90d-median precedence, sticky (only a real recent sale moves a price, else holds), money cards (kaboom/gold/gv/rare) uncapped to market, commons ceilinged (wash-guard), owner overrides/farce/keep preserved. **Brady Absolute Kaboom $3,100 -> $5,000** (all-time median was stale 2022 data) + 22 recent-sale moves; Tillman Red held $1,500; Brady Gold $50k/#1 $150k held. Pack EV $23.51 -> **$24.31**, live-verified. **(b)** poller `blockPulls` now gates on the `packcard-` card prefix instead of `run<=500` (pack token already excluded as burn_product action) — fixes count tracking for NFL + any high-numbered product. Vendored copy synced; PENDING: confirm Railway auto-redeployed the poller (root dir chain-poller) so NFL pulls start registering. PENDING BUILD (John approved): shared always-on auto-reprice engine for NFL + Silhouette (move per-sku value map to a DB table, scheduled fn reprices from chain_events on the recency rules, posts moves to /changes); Moonbirds already sales-driven via Diamond (align window); Soccer (pbc_engine) separate/later.
- **2026-08-11 (Auto-reprice engine B: built, pending migration 008 + seed)** — Moves the per-card value map + sales history out of static JSON into the DB so a scheduler reprices off live sales. BUILT: `migrations/008_pack_reprice.sql` (`pack_values` = DB value map, product+sku_base PK; `pack_sales` = running sales log w/ `dedup` unique key for idempotent appends). `tools/nflprizm-reprice.mjs` = the recency engine as a module (sticky: only a real recent sale moves a price, else holds; 3mo window precedence; owner OVERRIDE floors Brady Gold $50k / Mahomes $30k; FARCE=Landon Collins, KEEP=Pat Tillman; money classes uncapped to market, commons ceilinged) -- idempotency-tested (0 moves on already-applied data; Brady Kaboom holds $5k market, Tillman $1,500 hold). `tools/nflprizm-sales-seed.json` = 620 historical dated sales (versioned). `tools/seed-pack-reprice.mjs` = one-time seeder. REWROTE `netlify/functions/nflprizm-refresh.mjs` into the full hourly engine: append new chain_events (1584) sales -> pack_sales -> reprice -> persist changed pack_values -> log notable moves (>=$100 & >=15%) to price_changes -> recompute snapshot from live card_remaining + repriced values. DEPLOY-SAFE: falls back to the static value map until pack_values is seeded, so shipping before the migration only keeps current behavior. PENDING: John runs migration 008; then `node tools/seed-pack-reprice.mjs`; then validate the live function shows seeded=true. Same engine will extend to Silhouette next (its own reprice config).
- **2026-08-11 (Auto-reprice C: on-page feeds + Silhouette engine LIVE)** — C1: embedded the live price-change feed on the Silhouette + Moonbirds pages (mirrors the NFL page; fetch /api/changes filtered by product) so the change log shows on every pack page, not just /changes. Feed counts: nflprizm 13, moonbirds 12, silhouette 1. C2: extended the auto-reprice engine to Silhouette. Silhouette's historical sales log (527) is UNDATED so it can't be recency-windowed -- instead the engine reprices off the LIVE chain_events feed (prefixes 2217+2305, actively trading). `tools/silhouette-reprice.mjs` (Silhouette config: Cooper Flagg = owner FLOOR only-up 1/1 $40k / /10 $7,500; money cards = hits + scarce base run<25 price to market; base-pool commons run>=25 capped $50 vs washes). Seeded pack_values (product=silhouette, 1,736 from silhouette-values.json, base bool -> slot). REWROTE `netlify/functions/silhouette-refresh.mjs` into the full hourly engine (append chain sales -> reprice -> persist -> log moves -> recompute snapshot; reads base bool from slot for the recompute; deploy-safe static fallback). VALIDATED live: appended 135 chain sales, repriced 16 (Wemby Base Scope $14->$22, Cracked Ice capped $50, Dylan Harper Spotlight $250->$350, Flagg held), book $103.59 -> $104.08. NOTE: COMMON_CAP $50 may clip a few star base /25 parallels -- watch/refine. REMAINING: A poller-pull confirmation (watcher pending first NFL open); D pipe pack_values into Price My Card (/value).
- **2026-08-11 (Price My Card D: NFL + Silhouette added to /value)** — `netlify/functions/card-values.js` (/api/card-values) serves every pack_values card (NFL Prizm + Silhouette) as the valuer card shape {a,s,r,v,t:'S',m:'model',pack}; value.html boot() `mergeExtra()` concatenates them into CARDS after the soccer valuer loads, and showResult shows the pack name. Live-verified: /value now searches **11,317 cards** (soccer + 3,725 NFL + 1,736 Silhouette); Tom Brady Absolute Kaboom /99 renders "$5,000 · NFL Prizm" through the modeled-estimate path (factors ok, serial tiers hidden for /99), Cooper Flagg 1/1 $40k, values are the live auto-repriced ones. Route added. NOTE: Moonbirds values live in the moonbirds snapshot (not pack_values) so not yet in /value -- easy follow-on; Soccer already native to the page.
- **2026-08-11 (INCIDENT: poller Cloudflare 403 wall + resilience fix)** — The chain poller STALLED ~2h (cursor frozen at block 4217409); Railway logs = `apiGet retry after: HTTP 403` / `tick error: HTTP 403` looping. Cloudflare is blocking the poller's headless-browser reads on Railway's DATACENTER IP -- the documented risk ([[moonbirds-live-pipeline]]). Verified NOT our code (blockPulls unit-tested: catches the /6000 base pull, handles null sku, no throw). Impact while down: NFL/Silhouette COUNTS frozen + no NEW sales into the reprice engine (existing values stay live/correct; nothing updates). The old poller's inline retry re-hit Cloudflare on every 403, worsening it. FIX shipped to `chain-poller/poller.mjs`: POLL_MS 30s->120s, removed retry-hammering, main loop backs off (up to 10min) + rebuilds the browser session every 5 fails, and a **PROXY_SERVER/PROXY_USER/PROXY_PASS** hook on chromium.launch (residential proxy = the durable fix). If the block was rate-based it may decay under the gentler cadence; if it's a hard IP block the durable fix is a residential proxy (env vars only now), running the poller on a residential IP, or Diamond's CF-free node (localhost:8008). PENDING John: pick the durable IP path.
- **2026-09-02 (Floral FIX: page always fell back to baked -- bare <tbody> crashed render)** -- THE actual cause of the "page shows 9 / stale" reports: the /floral live fetch + valid() both passed (API returned live 654-pool data), but render() threw "Cannot set innerHTML of null" on $(baseBreak) -> caught -> page rendered the STALE BAKED FALLBACK_DATA (782 pool) every time. Root cause: my silhouette->floral clone left <tbody id=baseBreak> as a bare element OUTSIDE a <table>, which the HTML parser silently discards, so the element never existed in the DOM. Wrapped it in a hidden <table>. Page now renders the live snapshot (hub was already correct because its fetch doesn't call render()). Backend/poller/tracking were correct throughout.
- **2026-09-02 (Floral FIX: snapshot lagged behind live counts -> 5-min refresh)** -- John: page showed Yamal Cherry /9 = 9 while card_remaining said 7. Diagnosed: chain pull-tracking works (poller decrements card_remaining live, watched it update 21:57), but the PAGE reads the published floral_snapshots row which only refreshed @hourly -> frozen at the 20:00 view while packs kept ripping. Manually recomputed + PATCHed the snapshot to live counts (pool 654), and bumped floral-refresh schedule @hourly -> */5 * * * * so the page stays near-live on craft days. (The sales in-list query was already guarded, so it never blocked the recompute/PATCH.)
- **2026-09-02 (MLB FIX: FD slate label showed stale DK header)** -- After the slate-tab repaint fix, the slate HEADER chip still read the DK label ("6 Game Main 7:40 PM") on FanDuel, because #slate-chips renders from S.dkSlates (DK draftgroups only). Added renderSlateHeader(): on FD it shows a computed label from the FD slate own games ("FanDuel . N Games . <earliest first pitch>"), on DK the usual DK slate chips; wired into refreshAllRenders so it updates on platform swap.
- **2026-09-02 (MLB FIX: Slate tab not repainting on DK<->FD platform swap)** -- John: on FanDuel the Slate tab still showed the DraftKings games (fewer). Diagnosed live via console: platform=FD, S.dkTeams correctly = FD teams, and getSlateGames() correctly returned the 9 FD games -- but the DISPLAY was stale. Root cause: refreshAllRenders() (run after applyPlatformSwap) repainted hitters/pitchers/stacks/matchups/streaks but NOT the Slate tab (renderSlateGames). Added renderSlateGames(S.games) to refreshAllRenders so the slate game list repaints when the platform (and thus the slate's game set) changes. FD/DK slate data handling itself was already correct.
- **2026-09-02 (Floral pack ACTIVATED: seeded from live report, chain pull-tracking on)** -- Craft launched today; found the Floral pack on-chain = **packcard-2333** for Lotus /4, but Cherry /9 + Plum /18 are **packcard-2332** (shared with the main set) -- pool spans TWO prefixes. Collection report (17) has all 90 Floral SKUs + live UNCLAIMED counts. Seeded pack_values + card_remaining (product=floral, 90 rows each) from the report -> the vault-signal poller now decrements Floral remaining on every pull (Floral is NOT carved out). Confirmed John's rip: Messi Lotus /4 packcard-2333_494551_12718052_10 remaining=3 (serial #3 pulled), matching the 17:52 on-chain vault transfer. Current remaining-adjusted **book EV = $614.87** (UP from $595 full-pool: of 148 cards pulled so far most were cheap Plum /18, so the pool skews richer). Updated floral.html baked payload + packs-hub to $615. floral-refresh.mjs now queries chain_events by the exact 90 seeded SKUs (in-list, both prefixes) for sales repricing. REMAINING to fully auto-update the page: John runs migration 009_floral_snapshots -> Claude publishes the floral_snapshots row -> hourly floral-refresh recomputes from poller-decremented card_remaining. Tracking is live NOW; the page auto-updates once the snapshot table exists.
- **2026-08-31 (NEW PACK: World Cup Floral Edition -> /floral, live + auto-reprice pipeline)** -- New Panini craft pack (crafts 2026-09-02): 465 total 2-card packs from a 30-player floral checklist, parallels Lotus Flower /4, Cherry Blossom /9, Plum Blossom /18 (930 card-instances = 465 packs). VALUE MODEL (John): Lotus = player Gold price, Cherry = 50% Gold, Plum = /49 (Blue) price -- then cut to **20% of that** (John: "cut original by 80%"). Values derived from the main World Cup pbc_engine (ran it on the checklist to pull each player Gold/10 + Blue/49). PACK BOOK EV = **$594.97**, predicted ~$1,487; top chases Messi Lotus $10k / Yamal Lotus $7.2k. BUILT on the shared template (2-card single-pool variant: base_slots:0, hit_slots:2, hit_breakdown = the 3 parallels): floral.html (baked payload), netlify/functions/floral-data.js, routes (/floral, /api/floral-data), Floral nav link on all pack pages + packs-hub card, migration 009_floral_snapshots.sql. AUTO-REPRICE PIPELINE built like the others (dormant until secondary): tools/floral-recompute.mjs + tools/floral-reprice.mjs + netlify/functions/floral-refresh.mjs (@hourly, deploy-safe static fallback). ACTIVATION TODO (post Sept-2): set floral-refresh EV_FILTER to the real Floral packcard prefix + seed pack_values/card_remaining (product=floral); and run migration 009 then publish the floral_snapshots row to DB-back the page (works via baked payload until then).
- **2026-08-25 (MLB FIX: optimizer stranding multi-position players — Ohtani + Freeman couldn't both be used)** — John: locking Freddie Freeman (1B) and Ohtani together only seated Ohtani at 1B and dropped Freeman, even though Ohtani is OF-eligible. TWO bugs in mlb.html: (1) `posMatch()` OF branch did `['LF','CF','RF','OF'].includes(pos)` — an EXACT match, so DK's slash-joined multi-eligibility string "OF/1B" (Ohtani) matched 1B via `.includes` but FAILED OF. Rewrote posMatch to `split('/')` and test each eligible position, so multi-position players match every slot they qualify for. (2) `optBuildLineup` placed locked hitters greedy first-fit, so a flexible player (Ohtani OF/1B) grabbed the scarce 1B slot and stranded a rigid one (Freeman 1B-only). Replaced with **Kuhn's bipartite matching** so every locked hitter gets seated whenever a valid assignment exists (verified: Ohtani->OF, Freeman->1B even when Ohtani is processed first). Unit-tested + confirmed live in the page (posMatch OF/1B -> both true). Note: stack/remaining greedy fills still use first-fit (optimizer choices, not user locks) — fine for now.
- **2026-08-25 (MLB FIX: player injury-status loader was dead 6 weeks — statuses frozen at the v3 cutover)** — John: the MLB board showed Nick Kurtz (out for the year) as playable and Corey Seager (back from IL, hot) as still DL'd. ROOT CAUSE: `edge_player_status` froze **2026-07-09** — the status load (`loadPlayerStatuses()`) lived ONLY in the RETIRED `hitter-loader.js`, and the hitter-loader-v3 cutover (~07-10) orphaned it (v3 writes matchup/hot_history but never touched status). So every injury flag was 6 weeks stale (Seager's old D10 frozen; Kurtz never added). FIX (in the **edge-dfs-loader** repo, commit 317e57b): extracted it into `scripts/status-loader.js` (pull all 30 teams' fullRoster from statsapi, upsert status.code != 'A', delete rows not touched this run so activated players auto-clear) and made it the FIRST step in `daily-loader.yml` so it self-refreshes daily. Ran it manually to fix the live board immediately: 1,742 statuses refreshed, Kurtz -> D60, Seager cleared (active). NOTE: `matchup_cache`/`hot_history` verified CURRENT (an earlier "April-stale" read was an unordered-row artifact). `edge_prop_bets` IS genuinely stale (frozen 2026-06-02, separate dead loader not in the pipeline) but John confirmed props aren't in use -> left alone. See [[pitcher-loader-v4-cutover]].
- **2026-08-24 (Moonbirds OFF Diamond -> our chain engine + poller pull-detection FIXED + 3 pack UIs unified)** — John: Moonbirds pricing hadn't visibly moved in ~6 days and had dumped. Root cause: the Diamond-fed model treats standing OFFERS as floors that never discount, so a sales dump didn't move the book (frozen ~$128). MIGRATED Moonbirds onto our own chain pipeline (same engine as NFL/Silhouette), dropping the Diamond dependency: `tools/moonbirds-reprice.mjs` (recency engine; real mid-serial sale moves value either way so the dump shows through; /1 sale = the card; Birbhalla $2k global offer = hard floor; Base Silver commons capped) + `tools/moonbirds-recompute.mjs` (emits the SAME payload shape as Silhouette: pack_ev.base/hit_breakdown, cards_remaining — Moonbirds pack = 1 Base Silver slot + 1 weighted slot2, book = baseCard+hitCard). Seeded `pack_values` (133 cards, one-time from Diamond joined to our card_remaining) + backfilled `pack_sales` (421 chain sales); new hourly `netlify/functions/moonbirds-refresh.mjs` (chain-based, price>0 sales only) replaces the deleted Diamond `pbc-refresh.mjs`. **Book $128 (stale) -> $124.85** (predicted $312), now reflecting the commons dump. Moonbirds page rebuilt by cloning silhouette.html (unified template) -> all THREE pack pages (NFL, Silhouette, Moonbirds) now share one layout/CSS/render, differing only in per-pack facts. Also: Moonbirds now auto-lands in /value (reads pack_values). **POLLER PULL-DETECTION ROOT-CAUSED + FIXED:** counts were stale because `blockPulls` required a `burn_product` in-block, which under-counted EVERY product (burns aren't reliably co-located) and missed Moonbirds entirely (it NEVER burns — only 2 pulls tracked vs ~600 real opens). The real universal signal: a pull is a **$0 transfer_product FROM the pack-vault wallet `02882dfcdc4ab8076051922a738bd8914d19b4b4e053db6d860ce53e4ad8b91212`** (sources 96% of all pack $0-transfers across every product). Rewrote `blockPulls` (both `tools/` + `chain-poller/` copies) to key on the vault wallet — fixes count-tracking for ALL packs going forward. **Moonbirds is CARVED OUT of auto-decrement** (`BRIDGE_PREFIXES`) because it (+ Bad Eggs, untracked) allow bridging cards TO Ethereum, which also flows $0 from the vault and would masquerade as opens; Moonbirds counts kept accurate by re-seed instead (re-seeded card_remaining 3239 stale -> 2097 cards / ~1049 packs, matches John's 1023). TODO: pin the ETH-bridge tx signature to carve it out precisely, then Moonbirds can auto-track too. See [[moonbirds-live-pipeline]] [[poller-deploy-cadence]].
- **2026-08-22 (NEW TOOLS logged: browser extensions — pack_sniper + collection_bidder)** — Added PROJECT 5 documenting the three Panini Chrome extensions (they live on John's Desktop, outside this repo). `pack_sniper` (DONE earlier): scans pack listings, grabs a random 5th–15th-cheapest pack under a max price, stops at checkout for John to click BUY (money-moving stays human). `collection_bidder` (BUILT + working today): walks a collection A→Z auto-placing $1 × qty-5 / 60-day / PayPal Global offers on every card, skipping any within 30 burns of the 50%-useless line — offers auto-place since they move no money until a seller accepts. Reuses the OG `offer_placer` flow; new pieces = the quantity step + card-to-card auto-advance. Build took heavy DOM archaeology; the non-obvious selectors are captured in PROJECT 5 + the folder README so a fresh chat won't re-derive them (headline: the clickable card is `a.nft_link_wrapper` navigating to `packcard-*.html`, NOT `.nft_items`; virtualized grid needs `scrollRestoration='manual'` + scroll-to-top to keep A→Z order). Optional follow-up: a speed pass to trim the conservative waits. See [[collection-bidder-bot]].
- **2026-08-11 (RESOLVED: poller recovered; root cause = self-inflicted redeploys)** — The Cloudflare 403 stall CLEARED on its own once pushes stopped + the gentler poller (commit b1124a9) settled. Exact outage: block 4217409 -> 4217479, **20:18 -> 21:47 UTC (4:18 -> 5:47 PM ET Aug 11), ~89 min, 69 blocks skipped** (4217410-4217478; those blocks' sales/opens were jumped on catch-up since LOOKBACK=40 can't backfill a big gap). Healthy & steady since (~430 blocks through the next ~11h, no re-stall). **ROOT CAUSE (John's read, confirmed): self-inflicted by the rapid deploy flurry.** Every push to main redeploys chain-poller on Railway, and each redeploy restarts the headless browser = a FRESH Cloudflare challenge on startup; ~8 pushes in ~90 min hammered CF's challenge from the datacenter IP and tripped a RATE-based block. Not organic CF escalation, not a fragile pipeline. **TAKEAWAY / OPERATING RULE: do NOT rapid-fire deploys that touch the poller -- batch poller changes into one push and let it settle; normal untouched operation won't trip this.** Residential-proxy (PROXY_SERVER hook in poller.mjs) stays in-pocket ONLY for an unprovoked future block, not needed for normal running. Left the LOOKBACK=40 catch-up as-is (an 89-min self-inflicted gap isn't worth hardening if we avoid the trigger) -- optional future harden: dynamic lookback to backfill after any stall. STILL OPEN (not a bug): no pack-OPEN has landed since the run<=500->packcard- fix to visually confirm high-run pull capture; opens are infrequent/bursty (18h+ quiet stretches are normal), poller is current & ready; confirm later via card_remaining(nflprizm21) dropping below 196,837.
